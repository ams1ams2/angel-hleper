"""
إعدادات المشروع - الملاك المساعد
=====================================
جميع الثوابت والإعدادات العامة
"""

import os
from dotenv import load_dotenv

# =========================
# UI CONFIG
# =========================
CHARACTER_SIZE_W = 320
CHARACTER_SIZE_H = 420
TOP_BUBBLE_AREA = 150

COLORS = {
    "primary": "#7C3AED",   # بنفسجي
    "accent":  "#F59E0B",   # ذهبي
    "alert":   "#EF4444",   # أحمر
    "help":    "#22C55E",   # أخضر
}

# =========================
# OPENROUTER CONFIG
# =========================
load_dotenv()
OPENROUTER_API_KEY = os.getenv("OPENROUTER_API_KEY")
# ✅ لا نوقف التطبيق - يمكن إدخال API key لاحقاً عبر نافذة الإعدادات
# if not OPENROUTER_API_KEY:
#     raise RuntimeError("❌ OPENROUTER_API_KEY غير موجود في ملف .env")

MODEL = "openai/gpt-5.2"


def reload_api_key():
    """إعادة تحميل API key من ملف .env"""
    global OPENROUTER_API_KEY
    load_dotenv(override=True)  # override=True لإعادة التحميل
    OPENROUTER_API_KEY = os.getenv("OPENROUTER_API_KEY")
    return OPENROUTER_API_KEY

# =========================
# CAPTURE CONFIG
# =========================
CAPTURE_EVERY_SECONDS = 60
POLL_EVERY_SECONDS = 1
MIN_SECONDS_BETWEEN_CAPTURES = 4
MONITOR_INDEX = 1

# =========================
# MEMORY CONFIG
# =========================
MEMORY_PATH = "smart_memory.json"
MAX_HISTORY = 240
MAX_RECENT_HINTS = 10

# =========================
# PROMPTS
# =========================
SYSTEM_PROMPT_VISION = (
    "أنت نظام ذكي يعطي اقتراحات مدهشة ومتنوعة تدهش المستخدم، بناءً على الصورة فقط. "
    "قدّم الاقتراح مباشرة بجملة واحدة قصيرة بدون شرح أو مقدمات. "
    "نوّع المحتوى: اختصارات مفيدة، حقيقة مدهشة أو معلومة غريبة، نصيحة إنتاجية، تنبيه ذكي، "
    "فكرة لتوفير الوقت، معلومة صحية أو عن التركيز، أو اقتراح إبداعي مرتبط بما يظهر في الشاشة. "
    "لا تكتفِ بالاختصارات فقط — اخلط بين الاقتراحات العملية والمعلومات التي تثير الفضول. "
    "مهم جداً: إذا لاحظت أن المستخدم يتابع محتوى عن موضوع معيّن (مثلاً يوتيوب عن LLM أو الذكاء الاصطناعي، "
    "دورة برمجة، مقال عن تقنية، إلخ) فاعرف الموضوع وامنحه معلومة مهمة أو حصرية أو عميقة عن نفس الموضوع — "
    "مثلاً: «بما إنك تتابع عن LLM: [معلومة مهمة/نادرة عن LLM]» حتى يشعر أن المساعد يفهم سياقه ويضيف قيمة حقيقية. "
    "استخدم أسلوب واثق، ودود، وملهم. اكتب بالعربية فقط، جملة واحدة فقط، مع رمز تعبيري مناسب إن أمكن."
)

# اقتراحات احتياطية متنوعة عند فشل الرد (اختصارات + حقائق + نصائح)
FALLBACK_SUGGESTIONS = [
    "جرّب **Ctrl+L** للانتقال لشريط العنوان بسرعة ⚡",
    "هل تعلم؟ أخذ استراحة ٩٠ دقيقة بين المهام يرفع تركيزك 🧠",
    "**Win+Shift+S** يفتح أداة القص في ويندوز بدون برامج إضافية 📸",
    "شرب الماء بانتظام يقلل الإجهاد البصري أمام الشاشة 💧",
    "**Ctrl+W** يغلق التبويب الحالي بسرعة — احذر من الضغط بالخطأ 😄",
    "تحديد أوقات بدون إشعارات يضاعف إنتاجيتك حسب دراسات 📵",
    "**Alt+Tab** للتنقل بين النوافذ — أسرع من الماوس أحيانًا 🪟",
]

SYSTEM_PROMPT_CHAT = (
    "أنت مساعد شخصي عربي مختصر وواضح. "
    "جاوب مباشرة بدون مقدمات أو شرح طويل. "
    "استخدم جمل قصيرة ومتناسقة. "
    "أعطِ إجابة مختصرة (3-5 جمل كحد أقصى). "
    "استخدم رموز تعبيرية قليلة (1-2 فقط). "
    "تجنّب التكرار والتفاصيل الزائدة. "
    "اكتب بالعربية فقط، وأسلوب بسيط وواضح."
)
